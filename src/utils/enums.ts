export enum Role {
    USER = 'USER',
    MODERATOR = 'MODERATOR',
    ROOT = 'ROOT',
}

export enum MessageType {
    SUCCESS = 'success',
    ERROR = 'error',
    INFO = 'info',
}

export enum InputType {
    TEXT = 'text',
    EMAIL = 'email',
    PASSWORD = 'password',
}